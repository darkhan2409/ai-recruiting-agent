# HCB Recruiting Agent — пример переменных окружения
# Скопировать в .env и заполнить реальными значениями.
# .env в .gitignore — никогда не коммитить.

# Режим разработки: True → LLMExtractor и LLM-judge возвращают мок-данные.
# НЕ управляет источником ingestion (см. INGESTION_SOURCE ниже).
USE_MOCKS=True

# Источник ingestion:
# folder = FolderSource из INBOX_DIR (eval fixtures + reproducible demo).
# imap   = живой IMAPSource (требует IMAP_USER/PASSWORD).
# auto   = folder при USE_MOCKS=True, иначе imap (backwards-compat).
INGESTION_SOURCE=auto

# --- Инфраструктура (БЛОК 1) ---

# PostgreSQL (асинхронный драйвер asyncpg)
DATABASE_URL=postgresql+asyncpg://hcb:hcb@postgres:5432/hcb

# Qdrant
QDRANT_URL=http://qdrant:6333

# Расписание APScheduler
INGESTION_INTERVAL_SECONDS=3600
RETENTION_CRON_HOUR=3

# Версия приложения (для /health)
APP_VERSION=0.1.0

# --- Ingestion (БЛОК 2) ---

# IMAP (Yandex.Mail App Password). Пустые → используется FolderSource.
# Поддерживается только SSL (порт 993). Для plain IMAP (143) потребуется
# `MailBoxUnencrypted` — добавляется при необходимости.
IMAP_HOST=imap.yandex.ru
IMAP_PORT=993
IMAP_USER=
IMAP_PASSWORD=
IMAP_FOLDER=INBOX

# Хранилища (внутри контейнера)
INBOX_DIR=/app/storage/inbox
RESUMES_DIR=/app/storage/resumes

# Лимит вложения, MB
MAX_ATTACHMENT_MB=10

# --- Parsing (БЛОК 3) ---

# OpenAI ключ. Пустой → LLMExtractor работает в mock-режиме при USE_MOCKS=True.
OPENAI_API_KEY=
LLM_EXTRACT_MODEL=gpt-4o-mini
LLM_EXTRACT_TEMPERATURE=0.0

# --- Embeddings + Vector DB (БЛОК 4) ---

# Провайдер: e5 (fastembed, локальный ONNX) | openai (text-embedding-3-large)
# Модель e5-large ~2.24 GB, качается LAZY при первом poll и кэшируется в
# named volume `models_cache:/home/hcb/.cache`. Build-time остаётся быстрым.
EMBEDDER=e5
E5_MODEL=intfloat/multilingual-e5-large
# EMBEDDING_DIM удалён: dim вычисляется из активного embedder в init_collection.
OPENAI_EMBEDDING_MODEL=text-embedding-3-large
QDRANT_COLLECTION=resumes

# --- Заполняется в последующих БЛОКах ---

# roadmap: БЛОК 10 retention cleanup
# RESUME_RETENTION_DAYS=180
